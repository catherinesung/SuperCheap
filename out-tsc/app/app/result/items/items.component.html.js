-header > {};
{
    keywords;
}
/ion-header>
    < ion - list * ngFor;
"let item of items"[/** @class */ (function () {
    function class_1() {
    }
    return class_1;
}()).selected] = "item === selectedItem"(click) = "onSelect(item)" >
    {};
{
    item.id;
}
/span> {{item.name}}
    < /ion-list>
    < div * ngIf;
"selectedItem" >
    {};
{
    selectedItem.name | uppercase;
}
Details < /h2>
    < div > id;
/span>{{selectedItem.id}}</div >
    name;
;
"selectedItem.name";
placeholder = "name" >
    /label>
    < /div>
    < /div>;
//# sourceMappingURL=items.component.html.js.map